# Traces fan-out. Receives the canonical trace stream once over OTLP and mirrors
# it to every trace backend. All four accept OTLP natively.
receivers:
  otlp:
    protocols:
      grpc: { endpoint: 0.0.0.0:4317 }

processors:
  batch: {}

exporters:
  otlp/oteldb:
    endpoint: oteldb:4317
    tls: { insecure: true }
  otlp/tempo:
    endpoint: tempo:4317
    tls: { insecure: true }
  otlphttp/victoriatraces:
    traces_endpoint: http://victoriatraces:10428/insert/opentelemetry/v1/traces
    tls: { insecure: true }
  otlp/gigapipe:
    endpoint: gigapipe:4317
    tls: { insecure: true }

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters:
        - otlp/oteldb
        - otlp/tempo
        - otlphttp/victoriatraces
        - otlp/gigapipe
